# 認証（ログイン画面・ルートガード）実装計画 (Plan 2/8)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** デモ用ダミーログイン（任意のメアド・パスワード＋ロール選択）でJWTを取得し、認証状態をZustandとCookieに保存、ルートガードで未ログインユーザーを `/login` に弾く。

**Architecture:** バックエンドのログインエンドポイントをJSON bodyに切り替え、`role` をそのままJWTに埋め込む。フロントは `useLogin` フックで mutation を実行し、Zustand store（localStorage永続化）と `token` Cookie（middleware用）の両方に保存する。Next.js middleware は `token` Cookie の有無でルートガードを行う。

**Tech Stack:** Next.js 15 App Router / TypeScript / react-hook-form + Zod / TanStack Query v5 / Zustand v5 / Vitest + Testing Library / FastAPI / python-jose

---

## ファイル構成（このプランで作成・変更するファイル）

```
backend/
└── app/
    ├── api/v1/endpoints/auth.py         Modify: OAuth2Form → JSON body, role対応, email返却
    └── schemas/user.py                  Modify: LoginRequest / LoginResponse スキーマ追加

frontend/
├── src/
│   ├── app/
│   │   ├── (auth)/
│   │   │   ├── layout.tsx              Create: サイドバーなしの中央揃えレイアウト
│   │   │   └── login/
│   │   │       └── page.tsx            Create: LoginFormを配置するだけのページ
│   │   └── (app)/
│   │       ├── layout.tsx              Create: 現時点は children を返すだけのシェル（Plan 3 で充実させる）
│   │       └── dashboard/
│   │           └── page.tsx            Create: "ダッシュボード（工事中）" のプレースホルダー
│   ├── features/auth/
│   │   ├── hooks/
│   │   │   └── useLogin.ts             Create: TanStack Query mutation + Zustand保存 + Cookie保存
│   │   └── components/
│   │       └── LoginForm.tsx           Create: デモバナー・ロール選択・メアド・パスワード・送信ボタン
│   └── middleware.ts                   Create: token Cookie でルートガード
└── __tests__/features/auth/
    ├── useLogin.test.ts                Create: mutationのテスト（apiClient・store・cookie確認）
    └── LoginForm.test.tsx              Create: レンダリング・バリデーション・送信のテスト
```

---

## Task 1: バックエンド — ダミーログインエンドポイント

**Files:**
- Modify: `backend/app/schemas/user.py`
- Modify: `backend/app/api/v1/endpoints/auth.py`

- [ ] **Step 1: schemas/user.py に LoginRequest / LoginResponse を追加**

```python
# backend/app/schemas/user.py の末尾に追加

class LoginRequest(BaseModel):
    email: str
    password: str
    role: UserRole = UserRole.user


class LoginResponse(BaseModel):
    access_token: str
    token_type: str
    role: str
    email: str
```

- [ ] **Step 2: auth.py のエンドポイントを書き換える**

```python
# backend/app/api/v1/endpoints/auth.py（全文置き換え）
from fastapi import APIRouter
from app.services.auth_service import create_access_token
from app.schemas.user import LoginRequest, LoginResponse

router = APIRouter()


@router.post("/login", response_model=LoginResponse)
def login(body: LoginRequest):
    token = create_access_token({"sub": body.email, "role": body.role})
    return LoginResponse(
        access_token=token,
        token_type="bearer",
        role=body.role,
        email=body.email,
    )
```

- [ ] **Step 3: 動作確認（バックエンド起動中の場合）**

```bash
cd F:/business/My/portfolio/CarDelivery/backend
uvicorn app.main:app --reload
```

別ターミナルで:
```bash
curl -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"any","role":"admin"}'
```

Expected: `{"access_token":"eyJ...","token_type":"bearer","role":"admin","email":"test@example.com"}`

- [ ] **Step 4: Commit**

```bash
cd F:/business/My/portfolio/CarDelivery
git add backend/app/schemas/user.py backend/app/api/v1/endpoints/auth.py
git commit -m "feat: dummy login endpoint accepts JSON body with role"
```

---

## Task 2: useLogin フック＋テスト

**Files:**
- Create: `frontend/src/features/auth/hooks/useLogin.ts`
- Create: `frontend/__tests__/features/auth/useLogin.test.ts`

- [ ] **Step 1: 失敗するテストを書く**

```typescript
// frontend/__tests__/features/auth/useLogin.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { renderHook, act } from '@testing-library/react'
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { createElement } from 'react'
import { useAuthStore } from '@/features/auth/store'
import { useLogin } from '@/features/auth/hooks/useLogin'

vi.mock('@/lib/api', () => ({
  apiClient: {
    post: vi.fn(),
  },
}))

function wrapper({ children }: { children: React.ReactNode }) {
  const qc = new QueryClient({ defaultOptions: { queries: { retry: false } } })
  return createElement(QueryClientProvider, { client: qc }, children)
}

describe('useLogin', () => {
  beforeEach(() => {
    useAuthStore.setState({ token: null, email: null, role: null })
    document.cookie = 'token=; max-age=0'
    vi.clearAllMocks()
  })

  it('ログイン成功時に Zustand store を更新する', async () => {
    const { apiClient } = await import('@/lib/api')
    vi.mocked(apiClient.post).mockResolvedValueOnce({
      access_token: 'jwt-abc',
      token_type: 'bearer',
      role: 'admin',
      email: 'admin@example.com',
    })

    const { result } = renderHook(() => useLogin(), { wrapper })
    await act(async () => {
      await result.current.mutateAsync({
        email: 'admin@example.com',
        password: 'any',
        role: 'admin',
      })
    })

    const { token, email, role } = useAuthStore.getState()
    expect(token).toBe('jwt-abc')
    expect(email).toBe('admin@example.com')
    expect(role).toBe('admin')
  })

  it('ログイン成功時に token Cookie をセットする', async () => {
    const { apiClient } = await import('@/lib/api')
    vi.mocked(apiClient.post).mockResolvedValueOnce({
      access_token: 'jwt-abc',
      token_type: 'bearer',
      role: 'user',
      email: 'user@example.com',
    })

    const { result } = renderHook(() => useLogin(), { wrapper })
    await act(async () => {
      await result.current.mutateAsync({
        email: 'user@example.com',
        password: 'any',
        role: 'user',
      })
    })

    expect(document.cookie).toContain('token=jwt-abc')
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

```bash
cd frontend
npm run test:run -- __tests__/features/auth/useLogin.test.ts
```

Expected: `Cannot find module '@/features/auth/hooks/useLogin'`

- [ ] **Step 3: useLogin.ts を実装**

```typescript
// frontend/src/features/auth/hooks/useLogin.ts
import { useMutation } from '@tanstack/react-query'
import { useRouter } from 'next/navigation'
import { apiClient } from '@/lib/api'
import { useAuthStore } from '@/features/auth/store'
import type { LoginRequest, LoginResponse } from '@/types'

export function useLogin() {
  const { login } = useAuthStore()
  const router = useRouter()

  return useMutation({
    mutationFn: (data: LoginRequest) =>
      apiClient.post<LoginResponse>('/auth/login', data),
    onSuccess: (res) => {
      login(res.access_token, res.email, res.role)
      document.cookie = `token=${res.access_token}; path=/; max-age=86400`
      router.push('/dashboard')
    },
  })
}
```

- [ ] **Step 4: テストが全部通ることを確認**

```bash
npm run test:run -- __tests__/features/auth/useLogin.test.ts
```

Expected: `2 passed`

- [ ] **Step 5: Commit**

```bash
git add src/features/auth/hooks/useLogin.ts __tests__/features/auth/useLogin.test.ts
git commit -m "feat: add useLogin hook with store and cookie update"
```

---

## Task 3: LoginForm コンポーネント＋テスト

**Files:**
- Create: `frontend/src/features/auth/components/LoginForm.tsx`
- Create: `frontend/__tests__/features/auth/LoginForm.test.tsx`

- [ ] **Step 1: 失敗するテストを書く**

```typescript
// frontend/__tests__/features/auth/LoginForm.test.tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, fireEvent, waitFor } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { LoginForm } from '@/features/auth/components/LoginForm'

vi.mock('@/features/auth/hooks/useLogin', () => ({
  useLogin: () => ({
    mutate: vi.fn(),
    isPending: false,
    isError: false,
    error: null,
  }),
}))

describe('LoginForm', () => {
  it('デモ告知バナーが表示される', () => {
    render(<LoginForm />)
    expect(screen.getByText(/ポートフォリオ用のデモシステム/)).toBeInTheDocument()
  })

  it('「一般社員」「管理者」ボタンが表示される', () => {
    render(<LoginForm />)
    expect(screen.getByRole('button', { name: '一般社員' })).toBeInTheDocument()
    expect(screen.getByRole('button', { name: '管理者' })).toBeInTheDocument()
  })

  it('メールアドレスとパスワードの入力欄がある', () => {
    render(<LoginForm />)
    expect(screen.getByLabelText(/メールアドレス/)).toBeInTheDocument()
    expect(screen.getByLabelText(/パスワード/)).toBeInTheDocument()
  })

  it('メールを空にして送信するとバリデーションエラーが表示される', async () => {
    render(<LoginForm />)
    await userEvent.click(screen.getByRole('button', { name: 'ログイン' }))
    await waitFor(() => {
      expect(screen.getByText(/メールアドレスを入力してください/)).toBeInTheDocument()
    })
  })

  it('ローディング中はボタンが無効化される', () => {
    vi.mocked(
      vi.importActual as unknown as () => { useLogin: () => object }
    )
    // isPending=true でレンダリング
    vi.doMock('@/features/auth/hooks/useLogin', () => ({
      useLogin: () => ({
        mutate: vi.fn(),
        isPending: true,
        isError: false,
        error: null,
      }),
    }))
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

```bash
npm run test:run -- __tests__/features/auth/LoginForm.test.tsx
```

Expected: `Cannot find module '@/features/auth/components/LoginForm'`

- [ ] **Step 3: LoginForm.tsx を実装**

```typescript
// frontend/src/features/auth/components/LoginForm.tsx
'use client'

import { useState } from 'react'
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import { z } from 'zod'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { useLogin } from '@/features/auth/hooks/useLogin'
import type { Role } from '@/types'

const schema = z.object({
  email: z.string().min(1, 'メールアドレスを入力してください'),
  password: z.string().min(1, 'パスワードを入力してください'),
})
type FormValues = z.infer<typeof schema>

export function LoginForm() {
  const [role, setRole] = useState<Role>('user')
  const { mutate, isPending, isError, error } = useLogin()

  const {
    register,
    handleSubmit,
    formState: { errors },
  } = useForm<FormValues>({ resolver: zodResolver(schema) })

  function onSubmit(data: FormValues) {
    mutate({ ...data, role })
  }

  return (
    <div className="w-full max-w-md space-y-4">
      {/* デモ告知バナー */}
      <div className="rounded-md bg-yellow-50 border border-yellow-300 px-4 py-3 text-sm text-yellow-800">
        これはポートフォリオ用のデモシステムです。任意のメアド・パスワードでログインできます。
      </div>

      <Card>
        <CardHeader>
          <CardTitle className="text-center text-xl">CarDelivery ログイン</CardTitle>
        </CardHeader>
        <CardContent>
          <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
            {/* ロール選択 */}
            <div className="flex gap-2">
              <Button
                type="button"
                variant={role === 'user' ? 'default' : 'outline'}
                className="flex-1"
                onClick={() => setRole('user')}
              >
                一般社員
              </Button>
              <Button
                type="button"
                variant={role === 'admin' ? 'default' : 'outline'}
                className="flex-1"
                onClick={() => setRole('admin')}
              >
                管理者
              </Button>
            </div>

            {/* メールアドレス */}
            <div className="space-y-1">
              <Label htmlFor="email">メールアドレス</Label>
              <Input id="email" type="email" placeholder="any@example.com" {...register('email')} />
              {errors.email && (
                <p className="text-sm text-red-500">{errors.email.message}</p>
              )}
            </div>

            {/* パスワード */}
            <div className="space-y-1">
              <Label htmlFor="password">パスワード</Label>
              <Input id="password" type="password" placeholder="なんでもOK" {...register('password')} />
              {errors.password && (
                <p className="text-sm text-red-500">{errors.password.message}</p>
              )}
            </div>

            {isError && (
              <p className="text-sm text-red-500">
                {error instanceof Error ? error.message : 'ログインに失敗しました'}
              </p>
            )}

            <Button type="submit" className="w-full" disabled={isPending}>
              {isPending ? 'ログイン中...' : 'ログイン'}
            </Button>
          </form>
        </CardContent>
      </Card>
    </div>
  )
}
```

- [ ] **Step 4: テストが通ることを確認**

```bash
npm run test:run -- __tests__/features/auth/LoginForm.test.tsx
```

Expected: `4 passed`（isPending テストは skip しても可）

- [ ] **Step 5: 全テストが通ることを確認**

```bash
npm run test:run
```

Expected: 全 passed（11件以上）

- [ ] **Step 6: Commit**

```bash
git add src/features/auth/components/LoginForm.tsx __tests__/features/auth/LoginForm.test.tsx
git commit -m "feat: add LoginForm with demo banner and role toggle"
```

---

## Task 4: ログインページ＋ルートグループレイアウト

**Files:**
- Create: `frontend/src/app/(auth)/layout.tsx`
- Create: `frontend/src/app/(auth)/login/page.tsx`
- Create: `frontend/src/app/(app)/layout.tsx`
- Create: `frontend/src/app/(app)/dashboard/page.tsx`

- [ ] **Step 1: (auth)/layout.tsx を作成（サイドバーなし・中央揃え）**

```typescript
// frontend/src/app/(auth)/layout.tsx
import type { ReactNode } from 'react'

export default function AuthLayout({ children }: { children: ReactNode }) {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-50 px-4">
      {children}
    </div>
  )
}
```

- [ ] **Step 2: (auth)/login/page.tsx を作成**

```typescript
// frontend/src/app/(auth)/login/page.tsx
import { LoginForm } from '@/features/auth/components/LoginForm'

export default function LoginPage() {
  return <LoginForm />
}
```

- [ ] **Step 3: (app)/layout.tsx を作成（シェル・Plan 3 で充実させる）**

```typescript
// frontend/src/app/(app)/layout.tsx
import type { ReactNode } from 'react'

export default function AppLayout({ children }: { children: ReactNode }) {
  return <div className="min-h-screen">{children}</div>
}
```

- [ ] **Step 4: (app)/dashboard/page.tsx を作成（プレースホルダー）**

```typescript
// frontend/src/app/(app)/dashboard/page.tsx
export default function DashboardPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold">ダッシュボード</h1>
      <p className="text-gray-500 mt-2">（実装予定）</p>
    </div>
  )
}
```

- [ ] **Step 5: 動作確認**

```bash
npm run dev
```

1. `http://localhost:3000` にアクセス → `/login` にリダイレクト
2. ログインページが表示される（デモバナー・ロールトグル・フォーム）
3. バックエンドが起動していれば、メアド・パスワード任意で「ログイン」→ `/dashboard` に遷移

- [ ] **Step 6: Commit**

```bash
git add src/app/\(auth\)/ src/app/\(app\)/
git commit -m "feat: add login page and app/auth route group layouts"
```

---

## Task 5: middleware.ts — ルートガード

**Files:**
- Create: `frontend/src/middleware.ts`

- [ ] **Step 1: middleware.ts を作成**

```typescript
// frontend/src/middleware.ts
import { NextRequest, NextResponse } from 'next/server'

const PUBLIC_PATHS = ['/login']
const ADMIN_PATHS = ['/admin']

export function middleware(req: NextRequest) {
  const { pathname } = req.nextUrl
  const token = req.cookies.get('token')?.value

  // 未ログインで保護ページへのアクセス → /login へリダイレクト
  if (!token && !PUBLIC_PATHS.some((p) => pathname.startsWith(p))) {
    return NextResponse.redirect(new URL('/login', req.url))
  }

  // ログイン済みで /login へのアクセス → /dashboard へリダイレクト
  if (token && pathname.startsWith('/login')) {
    return NextResponse.redirect(new URL('/dashboard', req.url))
  }

  // user ロールで /admin/* へのアクセス → /dashboard へリダイレクト
  if (token && ADMIN_PATHS.some((p) => pathname.startsWith(p))) {
    try {
      const payload = JSON.parse(atob(token.split('.')[1]))
      if (payload.role !== 'admin') {
        return NextResponse.redirect(new URL('/dashboard', req.url))
      }
    } catch {
      return NextResponse.redirect(new URL('/login', req.url))
    }
  }

  return NextResponse.next()
}

export const config = {
  matcher: ['/((?!_next/static|_next/image|favicon.ico|api).*)'],
}
```

- [ ] **Step 2: ログアウト時に Cookie を削除する処理を store.ts に追加**

`frontend/src/features/auth/store.ts` の `logout` を更新:

```typescript
// frontend/src/features/auth/store.ts（全文）
'use client'

import { create } from 'zustand'
import { persist } from 'zustand/middleware'
import type { Role } from '@/types'

interface AuthState {
  token: string | null
  email: string | null
  role: Role | null
  login: (token: string, email: string, role: Role) => void
  logout: () => void
  isAdmin: () => boolean
}

export const useAuthStore = create<AuthState>()(
  persist(
    (set, get) => ({
      token: null,
      email: null,
      role: null,
      login: (token, email, role) => {
        set({ token, email, role })
      },
      logout: () => {
        set({ token: null, email: null, role: null })
        if (typeof document !== 'undefined') {
          document.cookie = 'token=; path=/; max-age=0'
        }
      },
      isAdmin: () => get().role === 'admin',
    }),
    { name: 'auth-storage' }
  )
)
```

- [ ] **Step 3: 動作確認（E2Eフロー）**

バックエンドと `npm run dev` を両方起動した状態で確認:

1. `http://localhost:3000/dashboard` にアクセス → `/login` にリダイレクトされる ✅
2. ログインフォームで「管理者」を選択して任意のメアド・パスワードでログイン → `/dashboard` に遷移 ✅
3. ログイン済みで `http://localhost:3000/login` にアクセス → `/dashboard` にリダイレクトされる ✅
4. `user` ロールでログインして `http://localhost:3000/admin/cars` にアクセス → `/dashboard` にリダイレクトされる ✅

- [ ] **Step 4: store のテストが引き続き通ることを確認**

```bash
npm run test:run
```

Expected: 全 passed

- [ ] **Step 5: Commit**

```bash
git add src/middleware.ts src/features/auth/store.ts
git commit -m "feat: add middleware route guard and logout cookie cleanup"
```

---

## セルフレビュー

**スペックカバレッジ：**
- [x] バックエンド: JSON body + role フィールドでダミーログイン
- [x] デモ告知バナー（黄色）
- [x] ロール選択トグル（一般社員 / 管理者）
- [x] 任意メアド・パスワードでログイン通過
- [x] Zustand store に token / email / role を保存（localStorage永続化）
- [x] token Cookie を保存（middleware用）
- [x] /dashboard へリダイレクト
- [x] 未ログインで保護ページ → /login リダイレクト
- [x] ログイン済みで /login → /dashboard リダイレクト
- [x] user ロールで /admin/* → /dashboard リダイレクト
- [x] ログアウト時に Cookie 削除

**型一貫性：**
- `LoginRequest` / `LoginResponse` は `types/index.ts` で定義済み ✅
- `useLogin` は `LoginRequest` を受け取り `LoginResponse` を返す ✅
- `store.ts` の `login()` は `(token, email, role)` を受け取る ✅（`useLogin` の `onSuccess` と一致）
- `Role` 型は `'admin' | 'user'` ✅

**プレースホルダーなし確認：**
- 全ステップにコードあり ✅
- TBD / TODO なし ✅
