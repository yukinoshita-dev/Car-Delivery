# フロントエンド基盤セットアップ実装計画 (Plan 1/8)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Next.js 16 フロントエンドに必要なパッケージ・フォルダ構成・型定義・APIクライアント・TanStack Query・Zustand Auth Store を整備し、後続フェーズが即座に着手できる基盤を作る

**Architecture:** `features/` 単位でモジュールを分離し、サーバー状態は TanStack Query、クライアント状態（認証情報）は Zustand で管理する。`lib/api.ts` が fetch ラッパーとして JWT ヘッダー付与を一元化する。テストは Vitest + Testing Library で実施。

**Tech Stack:** Next.js 16.2.4 / React 19 / TypeScript 5 / Tailwind CSS v4 / shadcn/ui / TanStack Query v5 / Zustand v5 / react-hook-form + Zod / Vitest + Testing Library

---

## ファイル構成（このプランで作成・変更するファイル）

```
frontend/
├── package.json                          # Modify: 新パッケージ追加
├── vitest.config.ts                      # Create: Vitest 設定
├── vitest.setup.ts                       # Create: testing-library setup
├── tsconfig.json                         # Modify: paths alias 追加
├── src/
│   ├── app/
│   │   ├── layout.tsx                    # Modify: QueryClientProvider + ZustandProvider 追加
│   │   └── page.tsx                      # Modify: /login へリダイレクト
│   ├── components/
│   │   └── ui/                           # Create: shadcn/ui コンポーネント（自動生成）
│   ├── features/
│   │   └── auth/
│   │       └── store.ts                  # Create: Zustand auth store
│   ├── lib/
│   │   ├── api.ts                        # Create: fetch ラッパー
│   │   └── queryClient.ts               # Create: TanStack Query クライアント
│   └── types/
│       └── index.ts                      # Create: Car / User / Reservation 型定義
└── __tests__/
    ├── lib/
    │   └── api.test.ts                   # Create: APIクライアントのテスト
    └── features/
        └── auth/
            └── store.test.ts             # Create: auth store のテスト
```

---

## Task 1: パッケージインストール

**Files:**
- Modify: `frontend/package.json`

- [ ] **Step 1: 依存パッケージをインストール**

`frontend/` ディレクトリで実行:

```bash
npm install @tanstack/react-query zustand react-hook-form @hookform/resolvers zod
```

- [ ] **Step 2: shadcn/ui の前提パッケージをインストール**

```bash
npm install class-variance-authority clsx tailwind-merge lucide-react
```

- [ ] **Step 3: テスト用パッケージをインストール**

```bash
npm install -D vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/user-event @testing-library/jest-dom
```

- [ ] **Step 4: インストール確認**

```bash
npm run dev
```

Expected: `localhost:3000` でエラーなく起動する

- [ ] **Step 5: Commit**

```bash
git add package.json package-lock.json
git commit -m "feat: install tanstack-query, zustand, react-hook-form, zod, vitest"
```

---

## Task 2: Vitest 設定

**Files:**
- Create: `frontend/vitest.config.ts`
- Create: `frontend/vitest.setup.ts`
- Modify: `frontend/package.json`（test スクリプト追加）

- [ ] **Step 1: vitest.config.ts を作成**

```typescript
// frontend/vitest.config.ts
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    setupFiles: ['./vitest.setup.ts'],
    globals: true,
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
})
```

- [ ] **Step 2: vitest.setup.ts を作成**

```typescript
// frontend/vitest.setup.ts
import '@testing-library/jest-dom'
```

- [ ] **Step 3: package.json に test スクリプトを追加**

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "test": "vitest",
    "test:run": "vitest run"
  }
}
```

- [ ] **Step 4: tsconfig.json に paths を追加**

```json
{
  "compilerOptions": {
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

- [ ] **Step 5: テストが動くか確認（ダミーテスト）**

`frontend/__tests__/smoke.test.ts` を一時作成:

```typescript
// frontend/__tests__/smoke.test.ts
import { describe, it, expect } from 'vitest'

describe('smoke', () => {
  it('vitest works', () => {
    expect(1 + 1).toBe(2)
  })
})
```

```bash
npm run test:run
```

Expected: `1 passed` と表示される

- [ ] **Step 6: ダミーテストを削除してコミット**

```bash
rm __tests__/smoke.test.ts
git add vitest.config.ts vitest.setup.ts package.json tsconfig.json
git commit -m "feat: setup vitest with jsdom and testing-library"
```

---

## Task 3: shadcn/ui 初期化 + テーマ設定

**Files:**
- Create: `frontend/components.json`（shadcn 設定・自動生成）
- Create: `frontend/src/components/ui/`（shadcn コンポーネント・自動生成）
- Modify: `frontend/src/app/globals.css`（CSS 変数）

- [ ] **Step 1: shadcn/ui を初期化**

```bash
npx shadcn@latest init
```

対話形式で以下を選択:
- Style: `Default`
- Base color: `Slate`
- CSS variables: `Yes`

- [ ] **Step 2: 必要なコンポーネントを追加**

```bash
npx shadcn@latest add button input label card badge select toast dialog form
```

- [ ] **Step 3: 動作確認**

```bash
npm run dev
```

Expected: コンパイルエラーなし

- [ ] **Step 4: Commit**

```bash
git add components.json src/components/ui/ src/app/globals.css
git commit -m "feat: initialize shadcn/ui with slate theme"
```

---

## Task 4: フォルダ構成整備

**Files:**
- Create: `frontend/src/features/auth/` (ディレクトリ)
- Create: `frontend/src/features/cars/` (ディレクトリ)
- Create: `frontend/src/features/reservations/` (ディレクトリ)
- Create: `frontend/src/features/dashboard/` (ディレクトリ)
- Create: `frontend/src/components/layout/` (ディレクトリ)
- Create: `frontend/src/types/index.ts`

- [ ] **Step 1: フォルダを作成**

```bash
mkdir -p src/features/auth/components src/features/auth/hooks
mkdir -p src/features/cars/components src/features/cars/hooks
mkdir -p src/features/reservations/components src/features/reservations/hooks
mkdir -p src/features/dashboard/components
mkdir -p src/components/layout
mkdir -p src/lib
mkdir -p src/types
mkdir -p __tests__/lib __tests__/features/auth
```

- [ ] **Step 2: types/index.ts を作成**

```typescript
// frontend/src/types/index.ts

export type Role = 'admin' | 'user'

export type CarStatus = 'available' | 'in_use' | 'maintenance'

export interface Car {
  id: number
  name: string
  license_plate: string
  capacity: number
  status: CarStatus
  total_mileage: number
}

export type ReservationStatus = 'pending' | 'approved' | 'in_progress' | 'completed' | 'cancelled'

export interface Reservation {
  id: number
  car_id: number
  car: Car
  user_id: number
  user: User
  start_datetime: string   // ISO 8601
  end_datetime: string     // ISO 8601
  destination: string
  purpose: string
  status: ReservationStatus
  mileage_driven: number | null
  created_at: string
}

export interface User {
  id: number
  email: string
  role: Role
}

export interface LoginRequest {
  email: string
  password: string
  role: Role
}

export interface LoginResponse {
  access_token: string
  token_type: string
  role: Role
  email: string
}

export interface DashboardToday {
  reservations: Reservation[]
}

export interface DashboardStats {
  car_stats: {
    car_id: number
    car_name: string
    reservation_count: number
    total_mileage: number
  }[]
}
```

- [ ] **Step 3: Commit**

```bash
git add src/features/ src/components/layout/ src/types/ __tests__/
git commit -m "feat: setup folder structure and type definitions"
```

---

## Task 5: APIクライアント（lib/api.ts）＋テスト

**Files:**
- Create: `frontend/src/lib/api.ts`
- Create: `frontend/__tests__/lib/api.test.ts`

- [ ] **Step 1: 失敗するテストを書く**

```typescript
// frontend/__tests__/lib/api.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest'

// Zustand store のモック
vi.mock('@/features/auth/store', () => ({
  useAuthStore: {
    getState: () => ({ token: 'test-token-123' }),
  },
}))

const mockFetch = vi.fn()
global.fetch = mockFetch

describe('apiClient', () => {
  beforeEach(() => {
    mockFetch.mockReset()
  })

  it('GETリクエストに Authorization ヘッダーを付与する', async () => {
    mockFetch.mockResolvedValueOnce({
      ok: true,
      json: async () => ({ data: 'ok' }),
    })

    const { apiClient } = await import('@/lib/api')
    await apiClient.get('/cars')

    expect(mockFetch).toHaveBeenCalledWith(
      expect.stringContaining('/cars'),
      expect.objectContaining({
        headers: expect.objectContaining({
          Authorization: 'Bearer test-token-123',
        }),
      })
    )
  })

  it('POSTリクエストに Content-Type と body を付与する', async () => {
    mockFetch.mockResolvedValueOnce({
      ok: true,
      json: async () => ({ id: 1 }),
    })

    const { apiClient } = await import('@/lib/api')
    await apiClient.post('/cars', { name: 'プリウス1号' })

    expect(mockFetch).toHaveBeenCalledWith(
      expect.stringContaining('/cars'),
      expect.objectContaining({
        method: 'POST',
        headers: expect.objectContaining({
          'Content-Type': 'application/json',
          Authorization: 'Bearer test-token-123',
        }),
        body: JSON.stringify({ name: 'プリウス1号' }),
      })
    )
  })

  it('レスポンスが ok でない場合は Error をスローする', async () => {
    mockFetch.mockResolvedValueOnce({
      ok: false,
      status: 401,
      json: async () => ({ detail: 'Unauthorized' }),
    })

    const { apiClient } = await import('@/lib/api')
    await expect(apiClient.get('/cars')).rejects.toThrow()
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

```bash
npm run test:run -- __tests__/lib/api.test.ts
```

Expected: `api.ts が見つからない` でエラー

- [ ] **Step 3: lib/api.ts を実装**

```typescript
// frontend/src/lib/api.ts
import { useAuthStore } from '@/features/auth/store'

const BASE_URL = process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:8000/api/v1'

function getHeaders(extra?: Record<string, string>): Record<string, string> {
  const token = useAuthStore.getState().token
  return {
    'Content-Type': 'application/json',
    ...(token ? { Authorization: `Bearer ${token}` } : {}),
    ...extra,
  }
}

async function request<T>(path: string, init: RequestInit = {}): Promise<T> {
  const res = await fetch(`${BASE_URL}${path}`, {
    ...init,
    headers: {
      ...getHeaders(),
      ...(init.headers as Record<string, string> | undefined),
    },
  })
  if (!res.ok) {
    const error = await res.json().catch(() => ({ detail: res.statusText }))
    throw new Error(error.detail ?? `HTTP ${res.status}`)
  }
  return res.json() as Promise<T>
}

export const apiClient = {
  get: <T>(path: string) => request<T>(path, { method: 'GET' }),
  post: <T>(path: string, body: unknown) =>
    request<T>(path, { method: 'POST', body: JSON.stringify(body) }),
  put: <T>(path: string, body: unknown) =>
    request<T>(path, { method: 'PUT', body: JSON.stringify(body) }),
  delete: <T>(path: string) => request<T>(path, { method: 'DELETE' }),
}
```

- [ ] **Step 4: テストが全部通ることを確認**

```bash
npm run test:run -- __tests__/lib/api.test.ts
```

Expected: `3 passed`

- [ ] **Step 5: Commit**

```bash
git add src/lib/api.ts __tests__/lib/api.test.ts
git commit -m "feat: add apiClient with JWT header injection and tests"
```

---

## Task 6: TanStack Query クライアント設定

**Files:**
- Create: `frontend/src/lib/queryClient.ts`
- Modify: `frontend/src/app/layout.tsx`

- [ ] **Step 1: queryClient.ts を作成**

```typescript
// frontend/src/lib/queryClient.ts
import { QueryClient } from '@tanstack/react-query'

export const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 1000 * 60,    // 1分間はキャッシュを新鮮とみなす
      retry: 1,
    },
  },
})
```

- [ ] **Step 2: app/layout.tsx に QueryClientProvider を追加**

```typescript
// frontend/src/app/layout.tsx
'use client'

import { QueryClientProvider } from '@tanstack/react-query'
import { queryClient } from '@/lib/queryClient'
import type { ReactNode } from 'react'
import './globals.css'

export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <html lang="ja">
      <body>
        <QueryClientProvider client={queryClient}>
          {children}
        </QueryClientProvider>
      </body>
    </html>
  )
}
```

- [ ] **Step 3: 動作確認**

```bash
npm run dev
```

Expected: コンパイルエラーなく起動する

- [ ] **Step 4: Commit**

```bash
git add src/lib/queryClient.ts src/app/layout.tsx
git commit -m "feat: setup TanStack Query client and provider"
```

---

## Task 7: Zustand Auth Store ＋テスト

**Files:**
- Create: `frontend/src/features/auth/store.ts`
- Create: `frontend/__tests__/features/auth/store.test.ts`

- [ ] **Step 1: 失敗するテストを書く**

```typescript
// frontend/__tests__/features/auth/store.test.ts
import { describe, it, expect, beforeEach } from 'vitest'
import { useAuthStore } from '@/features/auth/store'

describe('useAuthStore', () => {
  beforeEach(() => {
    useAuthStore.setState({ token: null, email: null, role: null })
  })

  it('初期状態では token / email / role がすべて null', () => {
    const { token, email, role } = useAuthStore.getState()
    expect(token).toBeNull()
    expect(email).toBeNull()
    expect(role).toBeNull()
  })

  it('login() で token / email / role が保存される', () => {
    useAuthStore.getState().login('jwt-token-abc', 'test@example.com', 'user')
    const { token, email, role } = useAuthStore.getState()
    expect(token).toBe('jwt-token-abc')
    expect(email).toBe('test@example.com')
    expect(role).toBe('user')
  })

  it('logout() で token / email / role がリセットされる', () => {
    useAuthStore.getState().login('jwt-token-abc', 'test@example.com', 'admin')
    useAuthStore.getState().logout()
    const { token, email, role } = useAuthStore.getState()
    expect(token).toBeNull()
    expect(email).toBeNull()
    expect(role).toBeNull()
  })

  it('isAdmin() は role が admin のとき true を返す', () => {
    useAuthStore.getState().login('token', 'admin@example.com', 'admin')
    expect(useAuthStore.getState().isAdmin()).toBe(true)
  })

  it('isAdmin() は role が user のとき false を返す', () => {
    useAuthStore.getState().login('token', 'user@example.com', 'user')
    expect(useAuthStore.getState().isAdmin()).toBe(false)
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

```bash
npm run test:run -- __tests__/features/auth/store.test.ts
```

Expected: `store.ts が見つからない` でエラー

- [ ] **Step 3: store.ts を実装**

```typescript
// frontend/src/features/auth/store.ts
import { create } from 'zustand'
import { persist } from 'zustand/middleware'
import type { Role } from '@/types'

interface AuthState {
  token: string | null
  email: string | null
  role: Role | null
  login: (token: string, email: string, role: Role) => void
  logout: () => void
  isAdmin: () => boolean
}

export const useAuthStore = create<AuthState>()(
  persist(
    (set, get) => ({
      token: null,
      email: null,
      role: null,
      login: (token, email, role) => set({ token, email, role }),
      logout: () => set({ token: null, email: null, role: null }),
      isAdmin: () => get().role === 'admin',
    }),
    {
      name: 'auth-storage',
    }
  )
)
```

- [ ] **Step 4: テストが全部通ることを確認**

```bash
npm run test:run -- __tests__/features/auth/store.test.ts
```

Expected: `5 passed`

- [ ] **Step 5: 全テストをまとめて実行**

```bash
npm run test:run
```

Expected: `api.test.ts 3 passed` + `store.test.ts 5 passed` の計 8 passed

- [ ] **Step 6: Commit**

```bash
git add src/features/auth/store.ts __tests__/features/auth/store.test.ts
git commit -m "feat: add Zustand auth store with persist and tests"
```

---

## Task 8: app/page.tsx を /login へリダイレクト

**Files:**
- Modify: `frontend/src/app/page.tsx`

- [ ] **Step 1: page.tsx をリダイレクトに変更**

```typescript
// frontend/src/app/page.tsx
import { redirect } from 'next/navigation'

export default function HomePage() {
  redirect('/login')
}
```

- [ ] **Step 2: 動作確認**

```bash
npm run dev
```

`http://localhost:3000` にアクセス → `/login` にリダイレクトされ 404 が表示される（/login はまだ未実装なので正常）

- [ ] **Step 3: Commit**

```bash
git add src/app/page.tsx
git commit -m "feat: redirect root to /login"
```

---

## セルフレビュー結果

**スペックカバレッジ：**
- [x] shadcn/ui 導入
- [x] フォルダ構成整備
- [x] 型定義（Car / User / Reservation / Role / LoginRequest / LoginResponse）
- [x] APIクライアント（JWT ヘッダー自動付与）
- [x] TanStack Query クライアント設定・Provider
- [x] Zustand Auth Store（login / logout / isAdmin / localStorage 永続化）
- [x] Vitest テスト環境
- [ ] 認証画面 → Plan 2
- [ ] ルートガード → Plan 2
- [ ] 共通レイアウト → Plan 3
- [ ] 各機能画面 → Plan 4〜7
- [ ] AWSデプロイ → Plan 8

**型一貫性：**
- `Role` 型を `types/index.ts` で定義 → `store.ts` でインポートして使用 ✅
- `useAuthStore.getState().token` を `api.ts` でインポートして参照 ✅
- `LoginRequest / LoginResponse` を `types/index.ts` で定義 → Plan 2 で使用 ✅
