# アプリシェル（レイアウト）実装計画 (Plan 3/8)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 認証済みページ全体を包むアプリシェル（折りたたみサイドバー・ヘッダー・UserDropdown）を実装し、`(app)/layout.tsx` に組み込む。

**Architecture:** Zustand `useUIStore` でサイドバーのcollapsed状態を管理。Sidebar・Header・UserDropdown を独立したコンポーネントとして作成し、AppShell が組み合わせる。ページタイトルは `usePathname()` + 静的マッピングで Header 内解決。

**Tech Stack:** Next.js 15 App Router / TypeScript / Tailwind CSS / shadcn/ui (DropdownMenu追加) / Lucide React / Zustand v5

---

## ファイル構成（このプランで作成・変更するファイル）

```
frontend/
├── src/
│   ├── features/ui/
│   │   └── store.ts                  Create: useUIStore（collapsed: boolean, toggleSidebar）
│   ├── components/layout/
│   │   ├── AppShell.tsx              Create: Sidebar + Header + main の外枠
│   │   ├── Sidebar.tsx               Create: 折りたたみサイドバー（role別ナビ）
│   │   ├── Header.tsx                Create: ページタイトル + UserDropdown
│   │   └── UserDropdown.tsx          Create: アバター → ドロップダウン（ログアウト）
│   ├── components/ui/
│   │   └── dropdown-menu.tsx         Create: shadcn add dropdown-menu
│   └── app/(app)/
│       └── layout.tsx                Modify: AppShell を組み込む
└── __tests__/components/layout/
    ├── Sidebar.test.tsx              Create: role別ナビ・トグルテスト
    └── Header.test.tsx               Create: タイトル表示テスト
```

---

## Task 1: useUIStore

**Files:**
- Create: `frontend/src/features/ui/store.ts`

- [ ] **Step 1: store.ts を作成**

```typescript
// frontend/src/features/ui/store.ts
import { create } from 'zustand'

interface UIState {
  collapsed: boolean
  toggleSidebar: () => void
}

export const useUIStore = create<UIState>()((set) => ({
  collapsed: false,
  toggleSidebar: () => set((state) => ({ collapsed: !state.collapsed })),
}))
```

- [ ] **Step 2: Commit**

```bash
cd F:/business/My/portfolio/CarDelivery/frontend
git add src/features/ui/store.ts
git commit -m "feat: add useUIStore for sidebar collapsed state"
```

---

## Task 2: shadcn DropdownMenu を追加

**Files:**
- Create: `frontend/src/components/ui/dropdown-menu.tsx`（shadcn が生成）

- [ ] **Step 1: shadcn add dropdown-menu を実行**

```bash
cd F:/business/My/portfolio/CarDelivery/frontend
npx shadcn add dropdown-menu
```

Expected: `src/components/ui/dropdown-menu.tsx` が生成される

- [ ] **Step 2: Commit**

```bash
git add src/components/ui/dropdown-menu.tsx package.json package-lock.json
git commit -m "chore: add shadcn dropdown-menu component"
```

---

## Task 3: UserDropdown コンポーネント

**Files:**
- Create: `frontend/src/components/layout/UserDropdown.tsx`

- [ ] **Step 1: UserDropdown.tsx を作成**

```typescript
// frontend/src/components/layout/UserDropdown.tsx
'use client'

import { useRouter } from 'next/navigation'
import { LogOut } from 'lucide-react'
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuSeparator,
  DropdownMenuTrigger,
} from '@/components/ui/dropdown-menu'
import { Badge } from '@/components/ui/badge'
import { useAuthStore } from '@/features/auth/store'

export function UserDropdown() {
  const { email, role, logout } = useAuthStore()
  const router = useRouter()
  const initial = email ? email[0].toUpperCase() : '?'
  const displayName = email ? email.split('@')[0] : ''

  function handleLogout() {
    logout()
    router.push('/login')
  }

  return (
    <DropdownMenu>
      <DropdownMenuTrigger asChild>
        <button className="flex items-center gap-2 rounded-md px-2 py-1 hover:bg-gray-100 transition-colors">
          <div className="w-8 h-8 rounded-full bg-blue-600 text-white flex items-center justify-center text-sm font-medium shrink-0">
            {initial}
          </div>
          <span className="text-sm font-medium hidden sm:block">{displayName}</span>
          <Badge
            variant={role === 'admin' ? 'default' : 'secondary'}
            className="hidden sm:block text-xs"
          >
            {role === 'admin' ? '管理者' : '一般社員'}
          </Badge>
        </button>
      </DropdownMenuTrigger>
      <DropdownMenuContent align="end" className="w-48">
        <div className="px-2 py-1.5 text-sm text-gray-500 truncate">{email}</div>
        <DropdownMenuSeparator />
        <DropdownMenuItem
          onClick={handleLogout}
          className="text-red-600 cursor-pointer focus:text-red-600"
        >
          <LogOut className="mr-2 h-4 w-4" />
          ログアウト
        </DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenu>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/layout/UserDropdown.tsx
git commit -m "feat: add UserDropdown with logout"
```

---

## Task 4: Header コンポーネント＋テスト

**Files:**
- Create: `frontend/src/components/layout/Header.tsx`
- Create: `frontend/__tests__/components/layout/Header.test.tsx`

- [ ] **Step 1: 失敗するテストを書く**

```typescript
// frontend/__tests__/components/layout/Header.test.tsx
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import { Header } from '@/components/layout/Header'

const mockPathname = vi.fn(() => '/dashboard')

vi.mock('next/navigation', () => ({
  usePathname: () => mockPathname(),
}))

vi.mock('@/components/layout/UserDropdown', () => ({
  UserDropdown: () => <div data-testid="user-dropdown" />,
}))

describe('Header', () => {
  it('/dashboard のとき「ダッシュボード」が表示される', () => {
    mockPathname.mockReturnValue('/dashboard')
    render(<Header />)
    expect(screen.getByText('ダッシュボード')).toBeInTheDocument()
  })

  it('/admin/approvals のとき「承認」が表示される', () => {
    mockPathname.mockReturnValue('/admin/approvals')
    render(<Header />)
    expect(screen.getByText('承認')).toBeInTheDocument()
  })

  it('UserDropdown が表示される', () => {
    mockPathname.mockReturnValue('/dashboard')
    render(<Header />)
    expect(screen.getByTestId('user-dropdown')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

```bash
cd F:/business/My/portfolio/CarDelivery/frontend
npm run test:run -- __tests__/components/layout/Header.test.tsx
```

Expected: `Cannot find module '@/components/layout/Header'`

- [ ] **Step 3: Header.tsx を実装**

```typescript
// frontend/src/components/layout/Header.tsx
'use client'

import { usePathname } from 'next/navigation'
import { UserDropdown } from './UserDropdown'

const PATH_TITLE_MAP: Record<string, string> = {
  '/dashboard': 'ダッシュボード',
  '/reservations/new': '配車予約申請',
  '/my-reservations': '自分の予約一覧',
  '/cars': '車両一覧',
  '/admin/approvals': '承認',
  '/admin/reservations': '予約管理',
  '/admin/cars': '車両管理',
  '/admin/users': 'ユーザー管理',
}

export function Header() {
  const pathname = usePathname()
  const title = PATH_TITLE_MAP[pathname] ?? ''

  return (
    <header className="h-14 border-b bg-white flex items-center justify-between px-6 shrink-0">
      <h1 className="text-lg font-semibold text-gray-900">{title}</h1>
      <UserDropdown />
    </header>
  )
}
```

- [ ] **Step 4: テストが通ることを確認**

```bash
npm run test:run -- __tests__/components/layout/Header.test.tsx
```

Expected: `3 passed`

- [ ] **Step 5: Commit**

```bash
git add src/components/layout/Header.tsx __tests__/components/layout/Header.test.tsx
git commit -m "feat: add Header with page title mapping and UserDropdown"
```

---

## Task 5: Sidebar コンポーネント＋テスト

**Files:**
- Create: `frontend/src/components/layout/Sidebar.tsx`
- Create: `frontend/__tests__/components/layout/Sidebar.test.tsx`

- [ ] **Step 1: 失敗するテストを書く**

```typescript
// frontend/__tests__/components/layout/Sidebar.test.tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { Sidebar } from '@/components/layout/Sidebar'
import { useAuthStore } from '@/features/auth/store'
import { useUIStore } from '@/features/ui/store'

vi.mock('next/navigation', () => ({
  usePathname: vi.fn(() => '/dashboard'),
}))

vi.mock('next/link', () => ({
  default: ({
    href,
    children,
    className,
  }: {
    href: string
    children: React.ReactNode
    className?: string
  }) => (
    <a href={href} className={className}>
      {children}
    </a>
  ),
}))

describe('Sidebar', () => {
  beforeEach(() => {
    useUIStore.setState({ collapsed: false })
  })

  it('userロールで user ナビ項目が表示される', () => {
    useAuthStore.setState({ token: 'tok', email: 'user@example.com', role: 'user' })
    render(<Sidebar />)
    expect(screen.getByText('配車予約申請')).toBeInTheDocument()
    expect(screen.getByText('自分の予約一覧')).toBeInTheDocument()
    expect(screen.queryByText('承認')).not.toBeInTheDocument()
    expect(screen.queryByText('予約管理')).not.toBeInTheDocument()
  })

  it('adminロールで admin ナビ項目が表示される', () => {
    useAuthStore.setState({ token: 'tok', email: 'admin@example.com', role: 'admin' })
    render(<Sidebar />)
    expect(screen.getByText('承認')).toBeInTheDocument()
    expect(screen.getByText('予約管理')).toBeInTheDocument()
    expect(screen.queryByText('自分の予約一覧')).not.toBeInTheDocument()
    expect(screen.queryByText('配車予約申請')).not.toBeInTheDocument()
  })

  it('トグルボタンクリックで collapsed が true になる', async () => {
    useAuthStore.setState({ token: 'tok', email: 'user@example.com', role: 'user' })
    render(<Sidebar />)
    const button = screen.getByRole('button', { name: 'サイドバーを折りたたむ' })
    await userEvent.click(button)
    expect(useUIStore.getState().collapsed).toBe(true)
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

```bash
npm run test:run -- __tests__/components/layout/Sidebar.test.tsx
```

Expected: `Cannot find module '@/components/layout/Sidebar'`

- [ ] **Step 3: Sidebar.tsx を実装**

```typescript
// frontend/src/components/layout/Sidebar.tsx
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'
import {
  CalendarDays,
  Car,
  CheckCircle,
  ChevronLeft,
  ChevronRight,
  LayoutDashboard,
  Plus,
  Users,
} from 'lucide-react'
import { cn } from '@/lib/utils'
import { useAuthStore } from '@/features/auth/store'
import { useUIStore } from '@/features/ui/store'
import type { LucideIcon } from 'lucide-react'

interface NavItem {
  label: string
  href: string
  icon: LucideIcon
}

const USER_NAV: NavItem[] = [
  { label: 'ダッシュボード', href: '/dashboard', icon: LayoutDashboard },
  { label: '配車予約申請', href: '/reservations/new', icon: Plus },
  { label: '自分の予約一覧', href: '/my-reservations', icon: CalendarDays },
  { label: '車両一覧', href: '/cars', icon: Car },
]

const ADMIN_NAV: NavItem[] = [
  { label: 'ダッシュボード', href: '/dashboard', icon: LayoutDashboard },
  { label: '承認', href: '/admin/approvals', icon: CheckCircle },
  { label: '予約管理', href: '/admin/reservations', icon: CalendarDays },
  { label: '車両管理', href: '/admin/cars', icon: Car },
  { label: 'ユーザー管理', href: '/admin/users', icon: Users },
]

export function Sidebar() {
  const { role } = useAuthStore()
  const { collapsed, toggleSidebar } = useUIStore()
  const pathname = usePathname()
  const navItems = role === 'admin' ? ADMIN_NAV : USER_NAV

  return (
    <aside
      data-collapsed={collapsed}
      className={cn(
        'relative flex flex-col h-screen bg-blue-900 text-white transition-all duration-200 shrink-0',
        collapsed ? 'w-16' : 'w-56'
      )}
    >
      {/* ロゴ */}
      <div className="h-14 flex items-center justify-center border-b border-blue-800 px-4 shrink-0">
        {collapsed ? (
          <Car className="h-6 w-6" />
        ) : (
          <span className="text-lg font-bold tracking-wide">CarDelivery</span>
        )}
      </div>

      {/* ナビ */}
      <nav className="flex-1 py-4 space-y-1 px-2">
        {navItems.map(({ label, href, icon: Icon }) => (
          <Link
            key={href}
            href={href}
            className={cn(
              'flex items-center gap-3 rounded-md px-3 py-2 text-sm font-medium transition-colors',
              pathname === href
                ? 'bg-blue-700 text-white'
                : 'text-blue-100 hover:bg-blue-800 hover:text-white'
            )}
          >
            <Icon className="h-5 w-5 shrink-0" />
            {!collapsed && <span>{label}</span>}
          </Link>
        ))}
      </nav>

      {/* トグルボタン */}
      <div className="border-t border-blue-800 p-2 shrink-0">
        <button
          onClick={toggleSidebar}
          className="w-full flex items-center justify-center rounded-md p-2 hover:bg-blue-800 transition-colors"
          aria-label={collapsed ? 'サイドバーを展開' : 'サイドバーを折りたたむ'}
        >
          {collapsed ? (
            <ChevronRight className="h-5 w-5" />
          ) : (
            <ChevronLeft className="h-5 w-5" />
          )}
        </button>
      </div>
    </aside>
  )
}
```

- [ ] **Step 4: テストが通ることを確認**

```bash
npm run test:run -- __tests__/components/layout/Sidebar.test.tsx
```

Expected: `3 passed`

- [ ] **Step 5: 全テストが通ることを確認**

```bash
npm run test:run
```

Expected: 全 passed

- [ ] **Step 6: Commit**

```bash
git add src/components/layout/Sidebar.tsx __tests__/components/layout/Sidebar.test.tsx
git commit -m "feat: add Sidebar with role-based nav and collapse toggle"
```

---

## Task 6: AppShell 組み立て＋レイアウト更新

**Files:**
- Create: `frontend/src/components/layout/AppShell.tsx`
- Modify: `frontend/src/app/(app)/layout.tsx`

- [ ] **Step 1: AppShell.tsx を作成**

```typescript
// frontend/src/components/layout/AppShell.tsx
import type { ReactNode } from 'react'
import { Sidebar } from './Sidebar'
import { Header } from './Header'

export function AppShell({ children }: { children: ReactNode }) {
  return (
    <div className="flex h-screen overflow-hidden">
      <Sidebar />
      <div className="flex flex-col flex-1 overflow-hidden">
        <Header />
        <main className="flex-1 overflow-y-auto bg-gray-50 p-6">
          {children}
        </main>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: (app)/layout.tsx を更新**

```typescript
// frontend/src/app/(app)/layout.tsx
import type { ReactNode } from 'react'
import { AppShell } from '@/components/layout/AppShell'

export default function AppLayout({ children }: { children: ReactNode }) {
  return <AppShell>{children}</AppShell>
}
```

- [ ] **Step 3: 全テストが通ることを確認**

```bash
npm run test:run
```

Expected: 全 passed

- [ ] **Step 4: 動作確認**

```bash
# バックエンド起動
cd F:/business/My/portfolio/CarDelivery/backend
source venv/Scripts/activate
uvicorn app.main:app --reload

# フロントエンド起動（別ターミナル）
cd F:/business/My/portfolio/CarDelivery/frontend
npm run dev
```

確認手順:
1. `http://localhost:3000` → `/login` にリダイレクト
2. 任意のメアド・パスワード、「一般社員」でログイン → ダッシュボードにサイドバー表示
3. サイドバーのトグルボタンで折りたたみ/展開が動く
4. ユーザードロップダウンでメールアドレス・ロールが表示される
5. ログアウトで `/login` に戻る
6. 「管理者」でログイン → admin ナビが表示される（承認・予約管理・車両管理・ユーザー管理）

- [ ] **Step 5: Commit**

```bash
git add src/components/layout/AppShell.tsx src/app/\(app\)/layout.tsx
git commit -m "feat: compose AppShell and wire into app layout"
```

---

## セルフレビュー

**スペックカバレッジ：**
- [x] 折りたたみサイドバー（w-56 ↔ w-16）
- [x] サイドバー背景 bg-blue-900 / アクティブ bg-blue-700
- [x] user/admin role別ナビ項目
- [x] ヘッダー: ページタイトル（pathname マッピング）+ UserDropdown
- [x] UserDropdown: アバター（イニシャル）+ 表示名 + ロールバッジ
- [x] ログアウト: Cookie削除 + /login リダイレクト
- [x] AppShell で全体を組み立て
- [x] (app)/layout.tsx に AppShell 組み込み

**型一貫性：**
- `useUIStore` の `collapsed` は `boolean` ✅
- `useAuthStore` から `email`, `role`, `logout` を取得 ✅（既存 store と一致）
- `NavItem` インターフェースで LucideIcon 型を使用 ✅
